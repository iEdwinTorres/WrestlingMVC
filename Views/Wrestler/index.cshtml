@model IEnumerable<WrestlerListItem>
@{
	ViewData["Title"] = "Wrestler Index";
}

<h1>Wrestler Index</h1>
<table class="table">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.Name)
			</th>
			<th>
				Active?
			</th>
			<th>
				Debut
			</th>
			<th>
				Retired
			</th>
			<th>
				Options
			</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					<a asp-action="Details" asp-route-id="@item.Id">@item.Name</a>
				</td>
				<td>
					@item.Status
				</td>
				<td>
					@item.Established?.ToString("MM/dd/yyyy")
				</td>
				<td>
					@item.Retired?.ToString("MM/dd/yyyy")
				</td>
				<td>
					<a asp-action="Edit" asp-route-id="@item.Id">Edit</a>
					<span>|</span>
					<a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
				</td>
			</tr>
		}
	</tbody>
</table>