@model PromotionDetail
@{
	ViewData["Title"] = Model.Name;
}

<h1>@Model.Name</h1>
<hr>

	<img src="@Model.Image" alt="@Model.Name Logo" height="400px">

<div>
	<a asp-action="Index">Back to List</a>
	<span>|</span>
	<a asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
</div>
